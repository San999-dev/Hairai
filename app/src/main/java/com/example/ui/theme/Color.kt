package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Warm Bio-Therapeutic Cream Palette
val CreamBackground = Color(0xFFFAF7F2)   // Soft Warm Cream Background
val WarmSurface = Color(0xFFF2EDE4)       // Warm Sand Secondary Backdrop
val SageDark = Color(0xFF5C8A5C)          // Professional Clinician Green (Primary)
val Sage = Color(0xFF8BAF8B)              // Gentle Herb Sage Green (Secondary)
val SageLight = Color(0xFFC4D9C4)         // Pastel Sage Accent
val GoldTexturizer = Color(0xFFC9A84C)    // Premium Gold Highlight Accent
val GoldLight = Color(0xFFF0E0A8)         // Warm Butter Gold Underlayer
val DeepBrown = Color(0xFF7A5C3A)         // Earthy Bark Brown
val BrownLight = Color(0xFFB8936A)        // Soft Walnut Cream-Brown
val CharcoalDark = Color(0xFF2C2C2C)      // Chic Charcoal (Body & Main Texts)
val MutedSlate = Color(0xFF888888)        // Neutral Muted Outline
val AlertRose = Color(0xFFD95C5C)         // Soft Coral Red Warning color
val CleanWhite = Color(0xFFFFFFFF)

// Semantic Theme Mappings to match existing configurations seamlessly
val BluePrimary = SageDark                 // Sage Dark acts as primary action
val GreenSecondary = Sage                  // Sage acts as secondary accent
val TealAccent = GoldTexturizer            // Gold acts as tertiary highlight
val WarmSlateLight = CreamBackground       // Main screen backgrounds
val DarkSlatePrimary = CharcoalDark        // Luxurious dark areas

// Pastel Backdrops mapping
val PastelBlue = Color(0xFFEBF3EB)         // Sage-touched white
val PastelGreen = Color(0xFFE8F5E8)        // Soft natural herb green backdrop
val PastelAmber = GoldLight                // Warm golden butter backdrop
val PastelTeal = SageLight

// Neutral mappings
val CarbonDark = CharcoalDark
val LightGrayBorder = WarmSurface
val MutedText = MutedSlate
